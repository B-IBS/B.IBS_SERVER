endpoint: ${env:PRISMA_NETWORK, "http://localhost:4466"}
datamodel:
  - database/datamodel.prisma

generate:
  - generator: typescript-client
    output: ./../src/generated/prisma-client/

hooks:
  post-deploy:
    - prisma generate
